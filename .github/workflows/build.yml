name: Build APK
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip openjdk-17-jdk \
            python3-pip autoconf automake libtool libtool-bin \
            pkg-config m4 cmake build-essential \
            zlib1g-dev libffi-dev libssl-dev

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install cython==0.29.37 buildozer

      - name: Patch p4a libffi to skip autoreconf
        run: |
          # Find p4a's libffi recipe and disable the autoreconf call
          # p4a uses a pre-generated configure script - we just need to stop it re-running autoconf
          P4A_PATH=$(python -c "import pythonforandroid; import os; print(os.path.dirname(pythonforandroid.__file__))")
          echo "p4a path: $P4A_PATH"
          LIBFFI_RECIPE="$P4A_PATH/recipes/libffi/__init__.py"
          echo "--- Original recipe ---"
          cat "$LIBFFI_RECIPE"
          # Replace autoreconf call with a no-op by patching the recipe
          sed -i 's/autoreconf -vif/echo skipping autoreconf/g' "$LIBFFI_RECIPE"
          # Also patch any shell scripts p4a calls
          find "$P4A_PATH/recipes/libffi/" -name "*.sh" -exec sed -i 's/autoreconf -vif/echo skipping autoreconf/g' {} \;
          echo "--- Patched recipe ---"
          cat "$LIBFFI_RECIPE"

      - name: Clean cache
        run: rm -rf ~/.buildozer

      - name: Build APK
        run: buildozer -v android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: bin/*.apk
